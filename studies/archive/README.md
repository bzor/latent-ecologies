# Archived Study records

Pre-contract Study records retired from the active `studies/` roster. Metadata only;
these directories never held versioned heavy media.

- `002-mass-flow/` — prototype-era Mass Flow study (`status: archived` in its
  study.json; schema `schemas/study.schema.json`). Its number was reused by the
  canonical `study_002_scar-tissue`, which lives in the active roster.
- `behavior-scar-tissue/` — pre-vault lab notes for the scar-tissue behavior
  (`lab-log.md` is still cited from the root README for measured solver behavior).

The active naming contract is `studies/study_NNN_slug/` (docs/STUDY_VAULT.md);
`studies/001-memory-field/` remains active in place as a grandfathered legacy vault.
