# Decisions

## 2026-08-22 — Behavior selections frozen

KC approved **A3 gap-4** and **C2 radius-3** in Discord message `1540841497015091301`.

- **A3 gap-4** is frozen as `component-behavior-28a9d187eece`: mirrored `RLRUUUL` / `LRLUUUR` walkers, four-cell initial separation, synchronous read-intent-commit scheduling, and same-destination pitch-apart resolution.
- **C2 radius-3** is frozen as `component-behavior-e31f9a6c8add`: six `RLRU` walkers seeded at radius three, synchronous read-intent-commit scheduling, and deterministic frame-exchange collision chemistry.
- Semantic field states remain behavioral scalar indices independent of palette or material.
- A3 opposed-roll and C2 radius-2 remain controls, not promoted selections.
- Behavior is now immutable for Look Development; aesthetic changes must not alter simulation semantics or selected cache hashes.

## 2026-08-23 — Supplemental A3 offshoot test

KC asked whether the highway pair could produce recognizable classic-ant-like offshoots in 3D. A bounded eight-run parameter study retained frozen A3 gap-4 as the unchanged control and tested published Hamann `RRLU` and `RUL` rule pairs across separation, heading, working-plane, and phase conditions.

- `RUL` gap-2, `RUL` gap-4, and `RUL` phase-1 passed the operational classic-like gate: exact translating tails for both walkers, distinct displacement vectors, shared-field rewrites, sparse occupancy, and three-axis aggregate extent.
- `RUL` gap-4 produced period-22 tails with 36 cross-lineage rewrites and one collision. `RUL` gap-2 reproduced the regime under a nearby separation with stronger interaction.
- `RRLU` reproduced strict period-32 highways but did not combine shared interaction, sparse branched morphology, and volumetric structure in the tested configurations.
- This supplement does not replace or mutate the promoted A3 gap-4 component. It is a Studio-composed pairing of published cubic 3D rules, not a rule-level novelty claim or strict equivalence claim for classic 2D `RL`.

## 2026-08-23 — Four-run structured bridge test

KC approved testing the unchanged `RUL` gap-4 control against relay nodes, ladder exchange, and scar branching. The three variants use deterministic feed-forward event writers that read primary walker positions without changing the primary field, frame, or trajectory.

- All four retain exact period-22 tails with the same two primary displacement vectors.
- Relay nodes write 124 persistent radial events across 12,000 steps. They preserve the carrier but read mainly as a denser ribbed surface.
- Ladder exchange writes 62 transverse events. It produces the clearest sparse repeated structural modulation and strongest balance between bridge continuity and added articulation.
- Scar branch writes 124 alternating side branches. State `0.5` marks the current active branch; state `1` preserves healed branches. It creates the strongest directional history and most visibly changes the silhouette.
- The tested mechanisms are secondary event-writer compositions. They do not prove that a causally perturbed primary walker can leave and reacquire the period-22 highway.
- Frozen A3 gap-4 and C2 radius-3 selections remain unchanged.

## 2026-08-23 — Causal recovery correction

A subsequent causal round replaced secondary feed-forward decoration with interventions applied to the primary field or complete primary body frames before synchronous reads.

- Relay-node increments the state beneath agent 0 at steps `3803`, `6403`, and `9003`, then applies the paired closing increment 22 steps later. Both final tails reacquire period 22; measured eight-cycle recovery latencies for agent 0 are 16, 16, and 88 steps.
- Ladder exchange swaps both walkers' complete `forward/up` frames at steps `3809`, `7009`, and `10209`, then swaps back after 88 steps. Both tails reacquire period 22. Shared rewrites increase from 36 to 81, and recovery latencies after the three closing swaps are `(0,486)`, `(0,0)`, and `(57,116)` steps.
- Scar recovery writes a four-cell branch on agent 0's local right axis at steps `4000`, `6200`, and `8400`, then restores the exact replaced values 22 steps later. Final trajectories and field match the control exactly.
- Ladder exchange is the strongest causal structural event. Scar recovery is exactly reversible but visually local. Relay-node redirects one highway while retaining period-22 translation.
- Round 08 remains a valid feed-forward event-writer control. Round 09 is the authoritative answer for causal excursion and highway reacquisition.

## 2026-08-23 — Relay held; C2 compact options reopened

KC placed `relay-node`, item 2 in the preceding ranked causal recommendation, on hold for later work in Discord message `1541180140439208067`. Its exact schedule, hashes, measurements, and artifact pointers are preserved in `held-branches.json`. It remains a non-promoted RUL alternate.

The same authorization reopened C2 radius-2 as a compact control and requested three creative parameter options. A bounded four-run round retained `RLRU`, six agents, frame-exchange collision chemistry, synchronous read-intent-commit, 1,200 steps, and microcell diagnostics.

- Radius-2 control exactly reproduces state hash `992fc62e45e85eec6b383735f79357fb44be42609df9d1e5865fff35ab7d2525` with 20 frame exchanges.
- Torsion cage changes only opposing body-frame rolls. It expands to `39 × 41 × 46`, but separates into planar fans and is the weakest compact option.
- Split core offsets one member of each opposing pair by one cell. It produces 26 frame exchanges and 13 collisions inside `27 × 20 × 22`, the densest compact transaction structure.
- Orbital cage changes inward headings to tangential circulation on the same radius-2 octahedron. It produces 33 frame exchanges around three recurring contested cells with exact `26 × 26 × 26` spans and measured nonplanarity `1.0`.
- Split core and orbital cage are recommended for comparison with the radius-2 control. All remain non-promoted alternatives; frozen C2 radius-3 is unchanged.

## 2026-08-23 — Prewarmed C2 continuation

KC retained the radius-2 control and requested a prewarmed, faster continuation of torsion cage plus two additional parameter options in Discord message `1541185609429684245`.

- Radius-2 control remains unchanged at steps 0 through 1,200 and reproduces its exact state hash.
- The other three movies hide a 600-step prewarm, then capture steps 600 through 2,400. Each contains 1,800 simulation steps in 12 seconds, 1.5 times the control step rate, sampled as 121 true snapshots without interpolation.
- Continued torsion cage grows into a central hub with three long translating arms across `102 × 76 × 112`. It is coherent but no longer compact.
- Torsion split combines opposed rolls with a one-cell stagger. It remains compact at `31 × 23 × 33` and is the strongest compact continuation.
- Orbital shear combines tangential headings with the one-cell stagger. It reaches an isotropic `66 × 66 × 66` span but resolves mainly into a bundled corridor with a terminal interaction mass.
- All three remain working alternatives. Frozen C2 radius-3 remains unchanged.

## 2026-08-23 — C2 radius-2 promoted; variants held

KC promoted item 1, the exact C2 radius-2 compact control, in Discord message `1541188371026546759`.

- `selection-c2-radius-2` is frozen as `component-behavior-4cacde29695d` with state hash `992fc62e45e85eec6b383735f79357fb44be42609df9d1e5865fff35ab7d2525`.
- Its immutable handoff retains `RLRU`, six radius-2 agents, frame-exchange collision chemistry, synchronous read-intent-commit, 1,200 steps, and 121 true snapshots.
- Existing promoted A3 gap-4 and C2 radius-3 components remain unchanged. Radius-2 is an additional promoted Look source.
- Items 2 and 4, prewarmed torsion cage and orbital shear, are held as non-promoted branches until after radius-2 Look Development.
- Their held records preserve exact seeds, state hashes, receipts, 600-step prewarm, 1,800-step capture, 12-second encoding, and the instruction to run each through the same bounded system for variations after Look Development.
