# Public field notebook

The website implementation begins after the local render vertical slice is
reliable. It will consume study manifests and generated publication packages
rather than duplicating their metadata manually.

